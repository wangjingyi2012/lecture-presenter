# 参考目录型表格

来源：`MCP协议/slide13.html`

这是表格模板族中的“目录 / 索引”变体。它的第一列描述对象，后续列描述该对象提供的能力、入口或关联资源。适合工具清单、接口目录、产品能力索引和资源参考页。

它不适合表达多个方案之间的优劣比较；那类内容使用 `table-comparison-matrix`。

预览数据位于 `slide.html` 的 `template-data` JSON 中。服务端渲染时替换结构化数据，不把模板规则下发给客户端。
