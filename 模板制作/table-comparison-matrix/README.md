# 横向方案对比表

来源：`MCP协议/slide22.html`

这是表格模板族中的“横向对比”变体。第一列固定为评价维度，后续列是候选方案。适合技术路线、协议版本、工具选型和方案权衡。

它不适合表达对象目录；对象目录使用 `table-reference-catalog`。

预览数据位于 `slide.html` 的 `template-data` JSON 中。服务端渲染时替换结构化数据，不把模板规则下发给客户端。
