# 架构总览到局部路径

来源：`AI大模型原理_安恒/slide24.html`、`Agent概念及基础开发/slide033.html`、`slide035.html`

适合复杂系统、模型或平台架构。页面先展示完整边界，再依次聚焦核心层、支撑层和端到端路径。它解决的是“先看全局，再理解局部如何协作”，不是普通的线性步骤图。

每层支持 2～4 个模块，路径支持 3～7 个节点。层通过 `role` 标记为 `context`、`core` 或 `support`，聚焦动作由角色决定；路径中的 ID 必须引用已存在模块。超过四层或需要精确物理连接坐标时，应改用专门的技术架构图模板。
